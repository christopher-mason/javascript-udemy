const names = "chris";

// functions - block of code to be invoked and run at anytime in our code

const greet = () => "Hello";

let resultOne = greet();
console.log(resultOne);

// methods -  different than functions in way we invoke them and where they are defined
// associated with object or data type
let resultTwo = names.toUpperCase();
console.log(resultTwo);