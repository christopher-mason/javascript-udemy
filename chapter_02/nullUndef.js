//let age; // not assigning value yet

// console.log(age, age + 3, `the age is ${age}`);

// let age = null; // could have on website where forms are to be filled out and then cleared

// console.log(age, age + 3, `the age is ${age}`); // the second instance of age will be zero (0+3). everything else = null.
